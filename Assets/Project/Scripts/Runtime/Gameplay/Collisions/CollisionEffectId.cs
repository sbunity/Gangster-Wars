namespace SBabchuk.Runtime.Gameplay.Collisions
{
    // Pool ids for collision/impact effects (indices into the Collisions pool).
    public static class CollisionEffectId
    {
        // Spawned where a bullet or melee attack lands.
        public const int Impact = 7;
    }
}
